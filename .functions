# Create a new directory and enter it
function mkd() {
    mkdir -p "$@" && cd "$@"
}

# Determine size of a file or total size of a directory
function fs() {
    if du -b /dev/null > /dev/null 2>&1; then
            local arg=-sbh
    else
            local arg=-sh
    fi
    if [[ -n "$@" ]]; then
            du $arg -- "$@"
    else
            du $arg .[^.]* *
    fi
}

# `o` with no arguments opens current directory, otherwise opens the given location
function o() {
    if [ $# -eq 0 ]; then
            open .
    else
            open "$@"
    fi
}

notebook(){
  python manage.py shell_plus --notebook --settings=project.settings-dev
}

myip(){
    ipconfig getifaddr en0
}

publicip(){
    curl ipecho.net/plain; echo
}

# Start djnago server
startwork(){
    cd /Users/akapatkar/financials && source .venv/bin/activate && ./up.sh
}

# Clean up wifi issue
fixwifi(){
    trash /Library/Preferences/SystemConfiguration
}

function tre() {
    tree -aC -I '.git|node_modules|bower_components' --dirsfirst "$@" | less -FRNX;
}
